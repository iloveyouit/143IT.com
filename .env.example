# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Set custom OpenAI model (default: gpt-4-turbo-preview)
# OPENAI_MODEL=gpt-4-turbo-preview

# n8n Webhook Configuration
# Contact form webhook URL - Create this webhook in your n8n instance
N8N_CONTACT_WEBHOOK=https://your-n8n-instance.com/webhook/contact

# Newsletter webhook URL - Create this webhook in your n8n instance
N8N_NEWSLETTER_WEBHOOK=https://your-n8n-instance.com/webhook/newsletter

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace 'your_openai_api_key_here' with your actual OpenAI API key
# 3. Replace the n8n webhook URLs with your actual n8n webhook endpoints
# 4. Never commit .env.local to version control (it's already in .gitignore)
